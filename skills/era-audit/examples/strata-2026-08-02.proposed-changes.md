# Proposed changes from the 2026-08-02 audit of this repository

Changes this audit proposes for files outside the skill itself, against commit
`b073dc98c2a02b8c0884e4ac4c70c1185c44ef08`. The audit is read-only and edited none of
them. Items 1 and 2 are applied; the rest are recorded and unapplied, which is what the
README's known-staleness note refers to.

The current audit froze commit `b073dc98c2a02b8c0884e4ac4c70c1185c44ef08` at `2026-08-02T17:44:30.719264Z`. These are the outside changes I would route next. This file records requests only; the era-audit work did not edit their targets.

1. In `commands/review.md:64-73` and `commands/review.md:405-408`, and in `commands/verify.md:140-143`, replace `--dangerously-bypass-approvals-and-sandbox` with exactly:

   ```text
   --sandbox read-only
   ```

   Keep a process deadline only when its value comes from measured local review latency or an operator deadline. The current `600` has no recorded basis. In `skills/codex-review/SKILL.md:194-203`, make the same replacement and add exactly this line after `codex exec`:

   ```text
   --ephemeral \
   ```

2. In `commands/review.md:111-114`, replace the visibility branch with exactly:

   ```text
   - Output `false` means public: run the public-repository privacy checks.
   - Output `true` means private: record that result and continue with the universal secret checks.
   - A command error or any other output means visibility is unknown: run the public-repository privacy checks conservatively and report the failed probe.
   ```

3. In `skills/browser-use/SKILL.md:53-68`, replace the routing section with exactly:

   ```text
   ## Tool selection

   Probe before routing. Use `browser-use` only when its executable resolves and its current `--help` output supports the command needed. Use Playwright MCP only when a Playwright MCP server responds in the running agent. An installed `playwright` or `browser-task` executable is a candidate backend, not proof of compatible syntax: read its live help before constructing commands. When no verified backend satisfies the task, report the missing capability instead of selecting an unavailable route.
   ```

   Apply the same preflight contract to `skills/mobile-preview/SKILL.md:13-18,56-112`, `commands/ship.md:44-53`, and `skills/frontend-design/SKILL.md:304`. Preserve the login-session, viewport-reset, before/after, and visual-inspection requirements. Regenerate command examples from live help; the audit intentionally does not invent replacement syntax.

4. In `skills/harness/SKILL.md:49,768-806,899-907`, replace the soft-warning-only policy with a required owner-selected hard pause budget. Keep convergence detectors for quality, but pause before more work when measured usage reaches the budget. When the runner exposes no exact token usage, record usage as unknown and require a measurable wall-time or spend budget instead. No default scalar is proposed because the repository contains no owner requirement or measurement that selects one.

5. In `commands/harness.md:6-7`, replace the absolute claim with exactly:

   ```text
   Cross-family evaluation can reduce correlated errors when `strong` is bound
   to a model family different from the generator.
   ```

6. In `README.md`, replace the `skills/` layout row with exactly:

   ```text
   skills/     procedural knowledge the agent loads on demand (spec, recon, harness, era-audit, ...)
   ```

   After `## Operating model`, insert exactly:

   ```text
   - **Audit the instruction era.** The `era-audit` skill probes current machine and house facts, freezes a declared instruction corpus, and writes quote-verifiable stale-prior findings without changing audited files.
   ```

## Mechanical wins already present

- The current metrics found zero standalone `<details>` openings across the 42 primary model-facing files. The earlier moving-tree snapshots measured 118 and then 86; those snapshots were pruned because they no longer describe the shipped example.
- The dead `codex-companion.mjs` harness path is gone. The current body dispatches through the configured `strong` lane and preflights its binding.
- The current 19 skill descriptions have a measured median of 311 characters and a maximum of 690. The initial moving-tree measurement had 44 descriptions with a median of 647 and a maximum of 1,791. Preserve distinctive trigger phrases during any later compression.

No `reference/INDEX.md` or `settings.json` change is needed. The skill is self-contained and its activation metadata ships beside it.
