# Observed primary defaults; native role files select their own models.
# Merge deliberately: this source installation permits full filesystem access.
model = "gpt-6-astra"
model_provider = "openai"
model_reasoning_effort = "medium"
service_tier = "default"
approval_policy = "never"
sandbox_mode = "danger-full-access"

[features]
memories = false

# Authentication comes from the local ChatGPT login, not this file.
# The optional DeepSeek/Codex bridge is a distinct metered route; see deepseek/.
