#!/usr/bin/env bash
# Session start hook - runs when an AI agent session begins.
# Customize this to set up session context, log session starts, etc.

set -euo pipefail

echo "Session started: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
